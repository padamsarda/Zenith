"""Standalone engineering utilities.

Not part of the Zenith assistant runtime — these are developer-facing
tools kept in this repository for convenience, structured so each can be
lifted out independently (e.g. into a future engineering-platform repo)
without touching `runtime/`, `shared/`, or `configs/`.
"""
