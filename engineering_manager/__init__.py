"""Engineering Manager: local-first orchestration of AI-performed
engineering work across projects, providers, and sessions.

A sibling application to the Zenith assistant runtime (`runtime/`), not
a part of it: this package depends only on `shared/` and the standard
library, never on `runtime/` or `configs/`. See
`docs/engineering_manager.md` for the architecture and
`architecture/` for the decision records behind it.
"""
